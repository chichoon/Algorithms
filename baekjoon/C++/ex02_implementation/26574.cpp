#include <iostream>

int    main(void)
{
    int    t, temp;
    std::cin >> t;
    for (int i = 0; i < t; i++) {
        std::cin >> temp;
        std::cout << temp << " " << temp << "\n";
    }
    return (0);
}
